\documentclass[tikz,border=10pt]{standalone}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc, backgrounds, fit}

\begin{document}
\begin{tikzpicture}[
    node distance=1.5cm and 3cm,
    every node/.style={font=\sffamily},
    excel_node/.style={
        rectangle,
        draw,
        fill=white,
        inner sep=0pt,
        minimum width=4cm,
        minimum height=1.2cm
    },
    python_node/.style={
        rectangle,
        draw,
        fill=white,
        inner sep=0pt,
        minimum width=5cm,
        minimum height=1.5cm
    },
    folder/.style={
        draw,
        fill=gray!5,
        dashed,
        inner sep=15pt,
        rounded corners
    }
]

% Styles pour les icônes
\newcommand{\excelicon}{\includegraphics[height=0.6cm]{excel_logo.pdf}}
\newcommand{\pythonicon}{\includegraphics[height=0.8cm]{python_logo.pdf}}

% --- BLOCS DE SORTIE ---
% Les blocs sont placés suffisamment à droite pour que les flèches output
% partent bien de script.east vers la droite.
\node[excel_node] (d1) at (16, 2) {
    \begin{tabular}{cl}
        \excelicon & \small homophobie\_50.xlsx
    \end{tabular}
};

\node[excel_node, below=0.3cm of d1] (d2) {
    \begin{tabular}{cl}
        \excelicon & \small obésité\_50.xlsx
    \end{tabular}
};

\node[excel_node, below=0.3cm of d2] (d3) {
    \begin{tabular}{cl}
        \excelicon & \small religion\_50.xlsx
    \end{tabular}
};

\node[excel_node, below=0.3cm of d3] (d4) {
    \begin{tabular}{cl}
        \excelicon & \small racisme\_50.xlsx
    \end{tabular}
};

\begin{scope}[on background layer]
    \node[
        folder,
        fit=(d1) (d4),
        label=above:Dossier: context
    ] (box_ctx) {};
\end{scope}

% Dossier nocontext sous le dossier context
\node[excel_node, below=2.5cm of d4] (e) {
    \begin{tabular}{cl}
        \excelicon & \small all\_200\_lines.xlsx
    \end{tabular}
};

\begin{scope}[on background layer]
    \node[
        folder,
        fit=(e),
        label=above:Dossier: nocontext
    ] (box_no) {};
\end{scope}

% --- CALCUL ET PLACEMENT DES BLOCS D'ENTRÉE ---
% Centre vertical global entre le haut du premier dossier et le bas du second.
\coordinate (vcenter) at ($(box_ctx.north)!0.5!(box_no.south)$);

\node[excel_node] (input) at (0, 0 |- vcenter) {
    \begin{tabular}{cl}
        \excelicon & \small CyberAggCorpus\_global.xlsx
    \end{tabular}
};

\node[python_node, right=3cm of input] (script) {
    \begin{tabular}{cl}
        \pythonicon & \bfseries\ttfamily prepare\_xlsx\_sample.py
    \end{tabular}
};

% --- FLÈCHES ---
% Entrée -> Script
\draw[-{Stealth[length=3mm]}, thick]
    (input.east) -- (script.west)
    node[midway, above] {input};

% Script -> Dossier context
\draw[-{Stealth[length=3mm]}, thick]
    (script.east) -- (box_ctx.west)
    node[midway, above, sloped] {output};

% Script -> Dossier nocontext
\draw[-{Stealth[length=3mm]}, thick]
    (script.east) -- (box_no.west)
    node[midway, below, sloped] {output};

\end{tikzpicture}
\end{document}